# Railway: set service root directory to `landing/` in the dashboard.
[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npx --yes serve dist -s -l ${PORT:-4173}"
restartPolicyType = "ON_FAILURE"
