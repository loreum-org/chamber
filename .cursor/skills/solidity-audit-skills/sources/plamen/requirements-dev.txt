# Test-only dependencies — pinned for reproducible, supply-chain-safe CI.
# NOT required at runtime (plamen install does not read this file); only the
# pytest test suite in scripts/ needs these. Versions pinned to exactly what the
# suite was authored/verified against on this machine.
-r requirements.txt
pytest==9.0.3
pytest-xdist==3.8.0  # `-n auto` fast-lane parallelism; already installed, pinned here
